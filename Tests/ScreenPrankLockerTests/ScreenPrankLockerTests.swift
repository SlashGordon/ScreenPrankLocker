import XCTest
@testable import ScreenPrankLocker

final class ScreenPrankLockerTests: XCTestCase {
    func testPlaceholder() {
        // Placeholder test — real tests added in later tasks.
        XCTAssertTrue(true)
    }
}
